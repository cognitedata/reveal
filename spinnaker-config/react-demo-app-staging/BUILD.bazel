load("//rules/spinnaker:spinnaker_pipeline.bzl", "spinnaker_pipeline")

spinnaker_pipeline(
    name = "spinnaker-config",
    app_config = "app-config.yaml",
    pipeline_configs = glob(
        ["*.yaml"],
        exclude = ["app-config.yaml"],
    ),
    visibility = ["//visibility:public"],
)

/* eslint-disable camelcase */
// Following types do not exist in the API, but were mocked to create the status page

export type ErrorDistributionResponse = {
  name: string;
  total_errors: number;
};

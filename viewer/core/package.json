{
  "name": "@reveal/core",
  "private": true,
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "yarn ws:build",
    "test": "yarn ws:test",
    "coverage": "yarn ws:coverage",
    "lint": "eslint . --ext .ts,.js --max-warnings 0",
    "lint:fix": "eslint . --ext .ts,.js --max-warnings 0 --fix"
  },
  "dependencies": {
    "@reveal/camera-manager": "workspace:*"
  },
  "sideEffects": false,
  "glslify": {
    "transform": [
      "glslify-import"
    ]
  }
}

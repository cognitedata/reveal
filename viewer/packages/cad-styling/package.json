{
  "name": "@reveal/cad-styling",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "start": "yarn ws:serve --env testFixture=CustomWithStyling.VisualTest.ts",
    "test": "yarn ws:test --config ./../../vitest.config.ts",
    "lint": "yarn ws:lint"
  },
  "dependencies": {
    "@reveal/utilities": "workspace:*"
  }
}

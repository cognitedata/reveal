{
  "name": "@reveal/pointclouds",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "start": "pnpm -w run ws:serve",
    "test": "vitest run --config ./../../vitest.config.ts",
    "run-wasm-pack": "pnpm -w run ws:update-cargo-index && wasm-pack",
    "test:wasm": "wasm-pack test --chrome --headless ./wasm",
    "lint": "eslint . --ext .ts,.js --max-warnings 0 --cache"
  },
  "dependencies": {
    "@reveal/data-providers": "workspace:*",
    "@reveal/metrics": "workspace:*",
    "@reveal/model-base": "workspace:*",
    "@reveal/pointcloud-styling": "workspace:*",
    "@reveal/rendering": "workspace:*",
    "@reveal/utilities": "workspace:*"
  }
}

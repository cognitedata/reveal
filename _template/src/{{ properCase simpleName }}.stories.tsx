import React from 'react';
import {{ properCase simpleName }}, { Props } from './{{ properCase simpleName }}';

export default {
  title: '{{ properCase simpleName }}',
};

export const Example = (props: Partial<Props>) => <{{ properCase simpleName }} {...props} />;

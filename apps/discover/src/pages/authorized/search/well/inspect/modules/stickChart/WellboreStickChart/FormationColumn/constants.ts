import { SCALE_PADDING } from '../../../common/Events/constants';

export const FORMATION_COLUMN_SCALE_CORRECTION = SCALE_PADDING + 32; // Column header height;

export const TOOLTIP_PLACEMENT = 'right';

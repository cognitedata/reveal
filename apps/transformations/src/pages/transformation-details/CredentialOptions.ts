export enum CredentialOptions {
  SingleOidc = 'single-oidc-credentials',
  SeparateOidc = 'separate-oidc-credentials',
  SingleApiKey = 'single-api-key',
  SeparateApiKey = 'separate-api-key',
  SeparateSession = 'separate-session',
}

/* eslint-disable no-underscore-dangle */
// config here is ONLY for local dev
window.__cogniteSidecar = {
  // FakeIdp is used for e2e tests fake authentication and fast login in local development
  fakeIdp: [],
  disableIntercom: true,
};

import { ComponentStory, ComponentMeta } from '@storybook/react';
import { MemoryRouter } from 'react-router-dom';

import Breadcrumb from './Breadcrumb';

import { Meta, Story } from '@storybook/addon-docs';

<Meta title="Components/Breadcrumb" />

# Breadcrumb

This is a sample description for the `Breadcrumb` component.

## Examples

### Basic

This is an example of a breadcrumb with one item.

<Story name="Basic">
  <MemoryRouter>
    <Breadcrumb items={[{ title: 'CDF Utilities' }, { path: '/storybook', title: 'Storybook' }]} />
  </MemoryRouter>
</Story>

### Empty

An example of an empty breadcrumb.

<Story name="Empty">
  <MemoryRouter>
    <Breadcrumb items={[]} />
  </MemoryRouter>
</Story>

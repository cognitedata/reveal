export enum SequenceDataType {
  BCTimeSeriesMap = 'Boundary Condition Time Series Map',
}

// XXX Duplicated constants from backend
//
// When listing of models is implemented in the backend this will be removed,
// and filter conditions will be handled in backend
export const DATA_TYPE_FILE = 'Simulator File';
export const DATA_TYPE_SEQUENCE = 'Boundary Condition Time Series Map';

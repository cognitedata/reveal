import styled from 'styled-components';

const TableOperations = styled.div`
  position: relative;

  button {
    margin-right: 8px;
  }
  button:nth-child(2) {
    position: absolute;
    right: 0;
  }
`;

export { TableOperations };

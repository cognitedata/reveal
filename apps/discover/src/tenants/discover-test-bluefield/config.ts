import config from '../discover-e2e-config';

/**
 * This config should be sync with `discover-e2-bluefield` project.
 * If there are changes to be made, add them to '../discover-e2e-config.ts'.
 */

const defaultConfig = {
  ...config,
};

export default defaultConfig;

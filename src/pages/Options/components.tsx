import styled from 'styled-components';
import { Colors } from '@cognite/cogs.js';

export const Card = styled.div`
  padding: 8px;
  border: 1px solid ${Colors.midgrey.hex()};
  border-radius: 8px;
  margin: 8px;
`;

/*!
 * Copyright 2021 Cognite AS
 */

import * as THREE from 'three';

describe('Camera Controls', () => {
  test('dummy', () => {
    const asd = new THREE.Vector3();
    expect(asd).toEqual(new THREE.Vector3());
  });
});

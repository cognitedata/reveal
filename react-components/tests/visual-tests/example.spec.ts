import { test, expect } from '@playwright/test';
import { loadStory } from './utils/loadStory';

test('cad model container storybook', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 1', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 2', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 3', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 4', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 5', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 6', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 7', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 8', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 9', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 10', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 11', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

test('cad model container storybook - 12', async ({ page }) => {
  await loadStory(page, 'example-primitivewrappers-cadmodelcontainer');
  await expect(page).toHaveScreenshot();
});

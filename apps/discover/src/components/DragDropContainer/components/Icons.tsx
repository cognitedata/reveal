import { Icon } from '@cognite/cogs.js';

export const IconDragHandleHorizontal = (props?: unknown) => (
  <Icon type="DragHandleHorizontal" {...props} />
);

export const IconDragHandleVertical = (props?: unknown) => (
  <Icon type="DragHandleVertical" {...props} />
);

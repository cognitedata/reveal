export type NotificationDestination = {
  destination: string;
};

export type NotificationConfig = {
  id: number;
  configId: number;
  destination: string;
};

export type NotificationConfigId = {
  id: number;
};

import { describe, test } from 'vitest';
import { CadInstanceMappingsCache } from './CadInstanceMappingsCache';

describe(CadInstanceMappingsCache.name, () => {
  const mock;

  describe('getMappingsForModelsAndInstances', () => {
    test("returns results from classic cache's getNodesForAssetIds method", () => {});
  });
});

/*!
 * Copyright 2019 Cognite AS
 */

/**
 * Style that allows overriding how a RootSectorNode (and its children)
 * are rendered.
 */
export type SectorRenderStyle = {
  showSectorBoundingBoxes?: boolean;
};

{
  "name": "@reveal/metrics",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "start": "pnpm -w run ws:start",
    "lint": "eslint . --ext .ts,.js --max-warnings 0 --cache"
  },
  "dependencies": {
    "@reveal/logger": "workspace:*"
  }
}

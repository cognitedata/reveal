import { Flex } from '@cognite/cogs.js';
import styled from 'styled-components';

export const Step2FormContainer = styled(Flex)`
  width: 50%;
`;

export const FlexGrow = styled.div`
  flex-grow: 1;
  width: 100%;
`;

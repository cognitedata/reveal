/*!
 * Copyright 2024 Cognite AS
 */

export enum VisibleState {
  All, // Visible
  Some, // Partly visible
  None, // None visible
  CanNotBeChecked, // Cab not be checked
  Disabled // Visiable disabled
}

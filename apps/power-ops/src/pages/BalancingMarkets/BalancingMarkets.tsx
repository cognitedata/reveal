import { Redirect } from 'react-router-dom-v5';
import { PAGES } from 'types';

export const BalancingMarkets = () => {
  // Placeholder component for navigation once we implement multiple balancing markets
  return <Redirect to={PAGES.RKOM_BID} />;
};

import { NumberInput, NumberInputProps } from '@data-exploration/components';

export const InternalIdFilter = (props: NumberInputProps) => {
  return <NumberInput label="Internal ID" {...props} />;
};

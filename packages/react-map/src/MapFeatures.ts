import { MapPopup } from './MapPopup';
import { UnmountConfirmationListner } from './UnmountConfirmation';
import { FloatingActionsPopup } from './FloatingActions/FloatingActionsPopup';

export const MapFeatures = {
  FloatingActions: FloatingActionsPopup,
  UnmountConfirmation: UnmountConfirmationListner,
  Popup: MapPopup,
};
